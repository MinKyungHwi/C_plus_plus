#include <iostream>
#include <stack>
#include <string>

using namespace std;

/*
int main()
{
	int N;
	cin >> N;

	stack<int> s;
	string command;

	
	for (int i = 0; i < N; i++)
	{
		cin >> command;
		if (command == "push")
		{
			int num;
			cin >> num;
			s.push(num);
		}
		if (command == "pop")
		{
			if (s.empty())
			{
				cout << -1 << "\n";
			}
			else
			{
				cout << s.top() << "\n";
				s.pop();
			}
		}
		if (command == "size")
		{
			cout << s.size() << "\n";
		}
		if (command == "empty")
		{
			if (s.empty())
				cout << 1 << "\n";
			else
				cout << 0 << "\n";
		}
		if (command == "top")
		{
			if (s.empty())
				cout << -1 << "\n";
			else
				cout << s.top() << "\n";
		}
	}
}
*/